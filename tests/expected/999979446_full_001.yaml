pdf: 999979446_full_001.pdf
notes: |
  Italian-language instrument with poor scan resolution. The pipeline is
  expected to produce an English-translated Survey, so `language.expect`
  is `en` (the post-translation target language). Structure is not pinned
  yet — add a `structure:` entry once item counts are confirmed.

exclude_from_testing: true
language: en
